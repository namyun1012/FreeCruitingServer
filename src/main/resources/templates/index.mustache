<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>hello html</title>
</head>
<body>

<h1>FreeCruiting</h1>
<div class = "col-md-12">
    <div class="row">
        <div class="col-md-6">
            <a href="/post/save" role="button" class="btn btn-primary">New Post</a>
            {{#userName}}
                Logged in as: <span id="user">{{userName}}</span>
                <a href="/logout" class="btn btn-info active" role="button">Logout</a>
            {{/userName}}
            {{^userName}}
                <a href="/oauth2/authorization/google" class="btn btn-success active" role="button">Google Login</a>
            {{/userName}}
        </div>
    </div>

    <div class="col-md-6">
        <div class="input-group">
            <select id = "search-type" class="form-select">
                <option value="all">통합 검색</option>
                <option value="title">제목</option>
                <option value="content">내용</option>
                <option value="author">작성자</option>
            </select>
            <input type="text" id="search-query" class="form-control" placeholder="검색어 입력">
            <button id="btn-search" class="btn btn-outline-primary">검색</button>
            <!-- js 구현, page 만들기 등 필요함 -->
        </div>
    </div>
    <!--
    <div class = "row">
        <div class = "col-md-6">
            <a href="/post/save" role="button" class="btn btn-primary">New Post</a>
        </div>
    </div>
    -->
    <br>
    <!-- 목록 출력 영역 -->
    <h2>최신 글</h2>
    <table class="table table-horizontal table-bordered">
        <thead class="thead-strong">
        <tr>
            <th>id</th>
            <th>title</th>
            <th>author</th>
            <th>updateDate</th>
            <th>type</th>
        </tr>
        </thead>
        <tbody id="tbody">
        {{#posts}}
            <tr>
                <td>{{id}}</td>
                <td><a href="/post/read/{{id}}">{{title}}</a></td>
                <td>{{author}}</td>
                <td>{{modifiedDate}}</td>
                <td>{{type}}</td>
            </tr>
        {{/posts}}
        </tbody>
    </table>
</div>

{{>layout/footer}}